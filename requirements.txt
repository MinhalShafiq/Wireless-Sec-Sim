# Phase 1 — 3D environment + ray tracing
sionna-rt==2.0.1
mitsuba==3.8.0
drjit==1.3.1

# Numerics / data
numpy>=1.26,<2.3
scipy>=1.11
matplotlib>=3.8
pandas>=2.1

# Geospatial (for OSM-derived custom scenes in Phase 1.5)
osmnx>=2.0
shapely>=2.0

# Phase 2 — scenario schema, configs, result IO
pydantic>=2.7
pyyaml>=6.0
pyarrow>=15.0

# Phase 4 — ML/AI layer
scikit-learn>=1.4
joblib>=1.4

# Phase 5 — vector retrieval
qdrant-client>=1.9

# Phase 6 — optimization (TPE + CMA-ES via Optuna)
optuna>=4.0
cmaes>=0.10

# Phase 7 — Streamlit dashboard
streamlit>=1.36
plotly>=5.20

# Dev / test
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
