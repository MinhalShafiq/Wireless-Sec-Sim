# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# --- Environments / secrets -----------------------------------------------
.env
.env.*

# --- IDE / OS -------------------------------------------------------------
.vscode/
.idea/
.DS_Store
.ipynb_checkpoints/

# --- Generated data (reproducible via `make repro`) -----------------------
# Everything under data/ except the .gitkeep marker — every subdirectory
# (runs/, sweeps/, studies/, datasets/, models/, embedders/, vector_store/,
# baselines/, validation/, scenes/) is regenerated by the pipeline.
data/*
!data/.gitkeep

# --- ML / RT caches -------------------------------------------------------
mlruns/
*.pt
*.pth
*.onnx
*.safetensors
*.exr
.optix_cache/

# --- Agent + private docs --------------------------------------------------
.claude/
design_proposal.md
project_details.md
